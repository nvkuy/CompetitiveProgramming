#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long t, n, s;
    cin >> t;
    while (t--) {
        cin >> n >> s;
        cout << s / (n * n) << endl;
    }

    return 0;
}
