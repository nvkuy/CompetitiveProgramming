#include <bits/stdc++.h>

using namespace std;

int main()
{

    scanf("%d %d", &n, &k);
    for (int i = 0; i < k; i++) {
        scanf("%d %d %s", &l, &r, s);
        if (s == "odd")
    }

    return 0;
}
