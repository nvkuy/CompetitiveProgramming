#include <bits/stdc++.h>

using namespace std;

vector<vector<int>> ys(101);

int main()
{

    int n, t1, t2;
    int xs[51], ys[51];
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d %d", &t1, &t2);

    }

    return 0;
}
